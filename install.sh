# install softwares
brew install neovim
brew install tmux
brew install zsh

brew install node
brew install nmp

brew install python3

# create symbolic links
ln -s nvim ~/.config/nvim/init.vim
ln -s tmux.conf ~/.tmux.conf
ln -s zshrc ~/.zshrc
